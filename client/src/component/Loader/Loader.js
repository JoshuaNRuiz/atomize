import React, {useState, useEffect, useRef} from 'react';

const Loader = (props) => {

    return (
        <div className='Loader'>
            <h1>Loading...</h1>
        </div>
    )
}

export default Loader;